services:
  bot:
    image: webutan/lain:latest
    container_name: japanese-learning-bot
    restart: unless-stopped
    volumes:
      - bot-data:/app/data
    ports:
      - "8765:8765"  # Anki sync API
    # Set DISCORD_TOKEN in your Docker GUI or .env file

volumes:
  bot-data:
